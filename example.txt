# 1. 创建环境
conda create -n reagent_vision python=3.10 -y
conda activate reagent_vision
pip install torch==2.0.1+cu117 torchvision==0.15.2+cu117 --extra-index-url https://download.pytorch.org/whl/cu117
pip install timm faiss-cpu fastapi uvicorn sqlalchemy aiosqlite opencv-python albumentations pydantic python-multipart tqdm scikit-learn

# 2. 把试剂图片放入 data/images/乙醇001/*.jpg 格式

# 3. 训练 + 建索引（5类以上再训）
python scripts/train.py
python scripts/build_index.py

# 4. 启动api
python -m uvicorn backend.api.main:app --host 0.0.0.0 --port 8000

# 5.启动前端
cd frontend
npm start

reagent-vision/
├── backend/
│   ├── config.py                    ← 所有超参数
│   ├── models/metric_model.py       ← EfficientNet + ArcFace 核心模型
│   ├── core/
│   │   ├── dataset.py               ← 数据集 + 增强策略
│   │   ├── trainer.py               ← 训练引擎（混合精度/早停/TensorBoard）
│   │   ├── recognition_engine.py    ← FAISS识别引擎（注册/检索）
│   │   └── database.py              ← SQLite数据库
│   └── api/main.py                  ← FastAPI全部接口
├── frontend/src/
│   ├── pages/ReagentRegister.jsx    ← 试剂录入界面
│   ├── pages/ReagentRecognize.jsx   ← 实时识别界面
│   └── pages/ReagentList.jsx        ← 试剂库管理
├── scripts/
│   ├── train.py                     ← 训练启动
│   ├── build_index.py               ← 构建FAISS索引
│   └── camera_test.py               ← 摄像头测试
└── docs/TUTORIAL.md                 ← 手把手教程